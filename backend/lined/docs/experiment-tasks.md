# Experiment Tasks

Use one pull request per task. Use the `experiment/` branch prefix for all
scientific experiment work.

| Branch name                                 | Type | Domain                 | Implemented           | Name                                      | Detailed description                                                                  | What should we expect as a final work                                               |
|---------------------------------------------|------|------------------------|-----------------------|-------------------------------------------|---------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| `experiment/backend-docs-foundation`        | Task | Backend foundation     | Yes                   | Backend docs and agent routing foundation | Add backend `AGENTS.md`, docs index, architecture/testing docs, and move API docs.    | Agents can navigate backend rules and documentation without reading the whole repo. |
| `experiment/backend-containerization`       | Task | Runtime infrastructure | Yes                   | Backend Docker image                      | Add Dockerfile and documented image build/run flow for Spring Boot backend.           | Backend can be built as a container image reproducibly.                             |
| `experiment/kind-postgres-backend-baseline` | Task | Runtime infrastructure | Yes                   | Minimal kind deployment                   | Add Kubernetes manifests for PostgreSQL and backend baseline deployment.              | Backend runs in local kind cluster and exposes health endpoint.                     |
| `experiment/backend-health-probes`          | Task | Runtime infrastructure | Yes                   | Kubernetes health probes                  | Add readiness/liveness probe configuration using Actuator endpoints.                  | Kubernetes can detect backend readiness and restart unhealthy pods.                 |
| `experiment/runtime-metrics-baseline`       | Task | Runtime infrastructure | Yes                   | Runtime metrics baseline                  | Expose Prometheus-compatible backend metrics and document key runtime signals.        | `/actuator/prometheus` can be collected for latency/error/resource analysis.        |
| `experiment/load-test-baseline`             | Task | Runtime infrastructure | Yes                   | Load-test baseline                        | Add first k6 or JMeter scenarios for users/lobbies/tasks/events workflows.            | Repeatable workload produces runtime metrics for baseline deployment.               |
| `experiment/hpa-resource-scenarios`         | Task | Runtime infrastructure | Yes                   | HPA and resource scenarios                | Add deployment variants for replicas, CPU/memory requests/limits, and HPA behavior.   | Experiments can compare deployment/runtime trade-offs.                              |
| `experiment/prometheus-telemetry-pipeline`  | Task | Runtime infrastructure | Yes                   | Prometheus telemetry pipeline             | Add a local Prometheus collection path for the kind backend, including scrape configuration and documentation for collecting Actuator runtime metrics. | Runtime metrics from the kind backend can be collected persistently enough for scenario comparison. |
| `experiment/runtime-scenario-summaries`     | Task | Runtime evidence       | Partial / branch-only | Runtime scenario summaries                | Add a repeatable workflow for running each deployment scenario under the selected k6 workload and producing sanitized `runtime-summary.json` artifacts. | Each scenario has comparable runtime summaries ready for collector ingestion.       |
| `experiment/runtime-provenance-manifest`    | Task | Runtime evidence       | Partial / branch-only | Runtime provenance manifest               | Add provenance metadata to runtime fitness outputs, including commit SHA, image tag, workload, telemetry window, configuration hash, constraint version, and fitness vector. | Runtime fitness results are traceable and reproducible for audit and paper evidence. |
| `experiment/scenario-fixture-discipline`    | Task | Runtime evidence       | Yes                   | Scenario fixture discipline               | Define explicit workload/context profiles and repeatable input setup for Lined experiment scenario runs. | Deployment/runtime comparisons use stable fixtures instead of manual setup.         |
| `experiment/slo-constraint-thresholds`      | Task | Runtime evidence       | Yes                   | SLO and constraint thresholds             | Define initial latency, error-rate, availability, restart, readiness, and resource-efficiency thresholds for classifying valid experiment variants. | Runtime evidence can be evaluated against explicit constraints instead of ad hoc interpretation. |
| `experiment/fitness-runtime-extension`      | Task | Runtime scoring        | Yes                   | Runtime fitness extension                 | Extend experiment documentation and/or collector design to include telemetry metrics. | Fixed CI fitness can be compared with runtime-aware adaptive fitness.               |
| `experiment/runtime-aware-scoring`          | Task | Runtime scoring        | Yes                   | Runtime-aware scoring                     | Add a versioned runtime fitness score that uses summarized runtime metrics while preserving the existing structural `fitnessScore`. | Runtime-aware scalar fitness can be computed without changing historical CI fitness semantics. |
| `experiment/adaptive-weighted-fitness`      | Task | Runtime scoring        | Yes                   | Adaptive weighted fitness                 | Implement context-sensitive weighting over structural and runtime signals for workload, SLO, or resource-pressure contexts. | Fixed structural fitness can be compared with an adaptive scalar fitness baseline.  |
| `experiment/pareto-optimization-baseline`   | Task | Runtime scoring        | Yes                   | Pareto optimization baseline              | Add a small NSGA-II or equivalent Pareto-based optimizer over the current deployment scenario set and runtime objectives. | Deployment variants can be compared as multi-objective trade-offs rather than a single weighted score. |
| `experiment/decision-usefulness-reporting`  | Task | Runtime scoring        | Yes                   | Decision-usefulness reporting             | Extend experiment reporting to compare whether Pareto/NSGA-II gives more actionable trade-off alternatives than fixed-weight scalar scoring. | Results explain decision usefulness in addition to numeric objective values.        |
| `experiment/experiment-results-reporting`   | Task | Runtime scoring        | Yes                   | Experiment results reporting              | Add result tables, plots, and documentation that summarize structural fitness, runtime summaries, adaptive scoring, and Pareto comparison outputs. | The repository contains article-ready experiment evidence for Results and Discussion. |
| `experiment/llm-support-service-plan`       | Task | LLM support            | Yes                   | LLM support service plan                  | Add documentation for a separate advisory LLM service, including scope boundaries, deployment options, sanitized inputs, versioned outputs, and review rules. | The backend roadmap explains how LLM assistance fits the experiment without becoming a CI gate or final architecture evaluator. |
| `experiment/llm-support-service-prototype`  | Task | LLM support            | No                    | LLM support service prototype             | Add a locally runnable or serverless-shaped helper that consumes requirements, ADR/SLO excerpts, and runtime-summary artifacts to propose candidate fitness rules. | Researchers can manually or periodically invoke a non-blocking support service and inspect advisory rule suggestions. |
| `experiment/llm-rule-review-workflow`       | Task | LLM support            | No                    | LLM rule review workflow                  | Add an approval workflow for classifying, validating, and promoting LLM-suggested candidate rules into versioned fitness configuration. | Candidate rules remain traceable to evidence and human review before affecting runtime-aware scoring. |
| `experiment/llm-rule-validation-checklist`  | Task | LLM support            | No                    | LLM rule validation checklist             | Add a checklist requiring source evidence, telemetry linkage, objective/constraint/context classification, threshold rationale, and expert approval for LLM-suggested rules. | LLM-assisted rule suggestions stay advisory and evidence-checked before promotion. |
| `experiment/llm-tradeoff-explanations`      | Task | LLM support            | No                    | LLM trade-off explanations                | Generate reviewed explanation drafts from experiment result artifacts, scenario comparisons, and Pareto trade-off outputs. | Article-ready explanations can accompany numeric results while evaluation remains telemetry-driven. |
| `experiment/llm-guardrail-evaluation`       | Task | LLM support            | No                    | LLM guardrail evaluation                  | Add checks for advisory-only scope, source linkage, telemetry linkage, objective/constraint/context classification, and human approval before rule promotion. | LLM-generated candidate rules cannot silently become CI gates, scoring inputs, or article claims without evidence and review. |
| `experiment/agent-evaluation-harness`       | Task | Agent support          | No                    | Agent evaluation harness                  | Define repeatable evaluation cases for Lined research agents using repo docs, Notion pages, runtime summaries, expected outputs, and review rubrics. | Agent-produced rule suggestions and summaries can be evaluated against fixed fixtures before influencing experiment work. |
| `experiment/agent-retrieval-routing`        | Task | Agent support          | No                    | Agent retrieval routing                   | Document and test retrieval order for Notion, backend docs, experiment artifacts, source-map entries, and web-search fallback. | Agents can prove they loaded the right context before proposing changes or durable research conclusions. |
| `experiment/agent-context-budget-reporting` | Task | Agent support          | No                    | Agent context budget reporting            | Require agent handoffs to include loaded context, omitted relevant context, assumptions, uncertainty, and source/synthesis boundaries. | Research-agent outputs become auditable and easier to review across long-running experiment threads. |
| `experiment/agent-feedback-dataset`         | Task | Agent support          | No                    | Agent feedback dataset                    | Create a lightweight dataset of accepted and rejected LLM rule suggestions, retrieved context, reviewer decisions, and rejection reason codes. | Prompt, retrieval, and evaluation improvements can be based on reviewed examples instead of ad hoc impressions. |
| `experiment/ai-support-observability`       | Task | Agent support          | No                    | AI support observability                  | Define metadata for LLM support runs, including model/tool, prompt version, retrieved sources, cost/latency when known, output type, failure mode, reviewer, and decision. | The advisory AI layer has operational evidence comparable across research runs without becoming a blocking runtime-fitness dependency. |
| `experiment/model-routing-and-caching-plan` | Task | Agent support          | No                    | Model routing and caching plan            | Document when to use smaller/faster models, retrieval-first responses, cached context packs, batch review, and escalation to stronger models for high-risk synthesis. | LLM support can be cost-aware and non-blocking while preserving stronger review for high-impact research outputs. |
| `bug/runtime-evidence-classification`       | Bug  | Architecture bug       | No                    | Runtime evidence classification depth     | Architecture review bug: runtime metric validation, SLO threshold meaning, missing-field handling, and scoring eligibility are split across `fitness-metrics-collector/scripts/collectMetrics.ts`, `docs/runtime-fitness-extension.md`, and `load-tests/runtime-scenarios/slo-thresholds-v1.json`.<br>Deepen the runtime evidence module so callers get one classified result instead of repeating threshold and eligibility knowledge in future scoring work. | Runtime evidence validation and classification live behind one tested interface that reports valid summary data, missing fields, SLO threshold version, and scoring eligibility for runtime-aware scoring. |
| `bug/lobby-access-seam`                    | Bug  | Architecture bug       | Yes                   | Lobby access seam depth                   | Architecture review bug: task, event, and lobby implementations duplicate private member/owner checks in `TaskServiceImpl`, `EventServiceImpl`, and `LobbyServiceImpl`, so callers know the lobby membership implementation and the lobby module interface stays shallow.<br>Deepen the lobby access seam with shared member/owner checks used by task, event, and lobby flows. | Lobby ownership and membership rules are concentrated in one tested module, and task/event/lobby flows no longer duplicate access logic. |
| `bug/domain-error-semantics`               | Bug  | Architecture bug       | Yes                   | Domain error semantics depth              | Architecture review bug: raw `NoSuchElementException`, `IllegalArgumentException`, and `SecurityException` leak from event, task, and lobby modules into generic error handling, while `EntityFinder` only unwraps `Optional` and leaves HTTP-safe semantics to each caller.<br>Deepen lookup and domain error handling so modules return application exceptions with consistent ProblemDetail behavior. | Not-found, bad-request, and forbidden cases use application error semantics through one seam, with tests proving raw Java exceptions no longer leak from the affected flows. |
| `bug/calendar-time-window-depth`           | Bug  | Architecture bug       | Yes                   | Calendar time-window depth                | Architecture review bug: event flows pass raw `OffsetDateTime` pairs and repeat start/end validation across create, update, list, conflict, and user-conflict paths, while overlap and conflict analysis stay embedded in `EventServiceImpl`.<br>Deepen calendar time-window handling and keep conflict analysis behind the event scheduling module interface. | Calendar operations use one validated time-window path, overlap/conflict rules have locality, and tests exercise scheduling behavior through the deepened interface. |
| `bug/scenario-runner-seam`                 | Bug  | Architecture bug       | Partial / branch-only | Scenario-runner seam                      | Architecture review bug: scenario evidence currently depends on procedural docs, manual `kubectl` ordering, k6 execution, telemetry queries, and hand-built summary output across `docs/hpa-resource-scenarios.md`, `load-tests/k6/load-test-baseline.js`, and `k8s/kind/scenarios/*`.<br>Create a reproducible scenario-runner seam when runtime-summary generation becomes implementation work. | A single scenario-runner interface accepts scenario, workload, and output directory, then coordinates adapters for Kubernetes, k6, telemetry, and collector-ready runtime summaries. |
| `bug/account-provisioning-policy`          | Bug  | Architecture bug       | Yes                   | Account provisioning policy depth         | Architecture review bug: `AccountApplicationServiceImpl` exposes provisioning booleans and hard-coded default role/free-plan policy, while role resolution knowledge is duplicated between user and role modules.<br>Deepen account provisioning so onboarding policy and role/default-plan decisions stay local. | Account registration has one clear provisioning path, default role/plan policy is concentrated, and tests no longer need to compose low-level boolean switches to verify onboarding behavior. |
