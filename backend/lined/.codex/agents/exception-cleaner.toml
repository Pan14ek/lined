name = "exception_cleaner"
description = "Refactoring agent focused on replacing raw Java exceptions with the application exception hierarchy."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
developer_instructions = """
Scan the `service` package implementations. Identify raw exceptions like `NoSuchElementException`, `IllegalArgumentException`, or `SecurityException`.
Refactor them to use the project's custom hierarchy inside `io.backend.lined.common.exception`. Ensure proper HTTP error mapping is preserved via Spring configuration.
"""