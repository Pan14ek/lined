{
  "name": "lined-metrics",
  "private": true,
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "metrics": "npm run build && node dist/collectMetrics.js"
  }
}
