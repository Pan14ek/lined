{
  "name": "lined-metrics",
  "private": true,
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "metrics": "npm run build && node scripts/collectMetrics.ts"
  }
}
