{
  "name": "lined-metrics",
  "private": true,
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "metrics": "npm run build && node dist/scripts/collectMetrics.js",
    "test": "npm run build && node --test dist/scripts/*.test.js"
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.1"
  }
}
